---
"treck": patch
---

Replace `--docs` and `--beautify` flags with unified `--format` option on `treck show`. Supports `mermaid` (default), `markdown`, `json`, and `ascii` formats. The new `json` format outputs structured graph data for AI agents.
