---
"treck": patch
---

Improve Inngest matcher: add step.sendEvent() detection, support array syntax for inngest.send(), update step.invoke() for new API with step names, add inngest-invoke resolution in graph builder, and add comprehensive tests
