---
"treck": patch
---

Support dark mode in mermaid dependency graphs. Render flowcharts as SVG with CSS variables that adapt to light/dark themes, and brighten default node fills for better visibility.
