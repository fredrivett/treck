---
"treck": patch
---

Fix node opacity dimming when nodes are focused. Node dimming now correctly applies when either selected or focused entries exist, and the async layout callback no longer overwrites dimmed state.
