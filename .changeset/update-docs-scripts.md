---
"treck": patch
---

Update README with missing `show` and `jsdoc` commands, add `--focus` option to `serve` docs, fix Contributing section to use pnpm, and add "View all commands" link to website landing page
